adjust 
@polkagate/extension-dapp version in packages/react-api/package.json

yarn build:www //to build the app, in the packages/apps/build
copy     packages/apps/build into ./docs
push


====================== to publish apps config=======

cd packages/apps-config/build

update package.json as follows  
    change "@polkadot/apps-config" to  "@polkagate/apps-config"
    adjust version
    
npm publish --auth-type=web --access public